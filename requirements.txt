# Pip fallback if you are not using pyproject.toml / pip install -e .
# Overview: keep in sync with [project.dependencies] in pyproject.toml.
httpx>=0.27
pydantic>=2.7
python-dotenv>=1.0
dnspython>=2.6
ruamel.yaml>=0.18
fastapi>=0.115
uvicorn[standard]>=0.30
itsdangerous>=2.2
