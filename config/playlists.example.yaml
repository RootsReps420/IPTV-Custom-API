# Copy to playlists.yaml (gitignored) and fill in real values.
#
# For a URL-check dry run you only need current_dns (and name).
# playlist_id / username / password are required for Discord swap tests and live failovers.
# current_dns should be the portal URL as EPGenius takes it (usually no port).

playlists:
  - name: account-1
    discord_id: "202010010127958016"
    playlist_id: "your-playlist-id"
    username: "your-username"
    password: "your-password"
    current_dns: "http://your-live-portal.example"
