# Copy to urls.yaml (gitignored). Shared standby pool.
# Replace these with real portal URLs you could swap to.
# They are probed every cycle; only currently healthy ones are swap candidates.

available:
  - "http://your-standby-1.example"
  - "http://your-standby-2.example:8080"
