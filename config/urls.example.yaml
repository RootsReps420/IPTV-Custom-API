# Copy to urls.yaml (gitignored). Shared standby pool.
# Each URL is probed every cycle. Only currently healthy, non-Cloudflare-proxied hosts are swap candidates.

available:
  - "http://your-standby-1.example"
  - "http://your-standby-2.example"
