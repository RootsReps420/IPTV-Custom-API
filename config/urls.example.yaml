# Copy to urls.yaml (gitignored). Shared standby pool.
# Each URL is probed every cycle. Swap picks a healthy host: no Cloudflare first, then CF NS, then CF proxy.

available:
  - "http://your-standby-1.example"
  - "http://your-standby-2.example"
